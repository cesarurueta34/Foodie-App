</br>
 <a href="/" class="btn btn-secondary">Back</a>
 <a href="/wishlist" class="btn btn-secondary">Wishlist</a>
<a href="/new" class="btn btn-primary">Add a Place!</a>
<header class="d-flex justify-content-between align-items-center">
  <h1>Visited</h1>
</header>

<div class="row">
    {{#each visited}}
        <div class="d-flex flex-column col-4 card" >
            <h5 class="card-title">{{this.name}}</h5>
            <div class="card-body d-flex flex-column justify-content-between">
                <img alt='restaurant image' class="card-img"  src="{{this.image}}" alt="food image">
                </br>
                <div>
                        <a href="/edit/{{this.id}}" class="btn btn-primary">Edit</a>
                        <a href="/details/{{this.id}}" class="btn btn-primary">Details</a>
                        
                       <form action="/{{this.id}}?_method=DELETE" method="POST" class="d-inline">
                            <input type="submit" value="Delete" class="btn btn-danger" />
                        </form>
                </div>
            </div>
        </div>
    {{/each}}
</div>